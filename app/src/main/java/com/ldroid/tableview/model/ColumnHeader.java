package com.ldroid.tableview.model;

public class ColumnHeader extends Cell {
    public ColumnHeader(String mId, String mText) {
        super(mId, mText);
    }

    public ColumnHeader(String mId, String mText, int mWidth) {
        super(mId, mText, mWidth);
    }
}
